name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - name: Install cloc
        run: sudo apt-get update && sudo apt-get install -y cloc
      - name: Check formatting
        shell: bash
        run: |
          files="$(gofmt -l .)"
          if [ -n "$files" ]; then
            printf '%s\n' "$files"
            exit 1
          fi
      - name: Vet
        run: go vet ./...
      - name: Check source size
        run: make agent-sloc app-sloc
      - name: Test
        run: go test -race -shuffle=on ./...
      - name: Build
        run: go build -o "$RUNNER_TEMP/dr-charm" ./cmd/dr-charm
